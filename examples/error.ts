let x: number = 3 x;
let y: number = 3;
for(x=0;x<4) {
    console log("test");
    log("okay" as string);
}

class X<T> extends Array<T> {
    export let i = 4;
    lala(test: number): number {
        let x: string = "a";
        let y: string "b" "c"
        let z: string = "c"
    }

    3+4

    xx(a: number)() {}

    another(a: number) []
    abstract sdf(): void;
}